eureka:
  service-url:
    defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: feign-consumer
server:
  port: 8003
management:
  endpoints:
    web:
      exposure:
        include: '*'

